{
  "name": "scanwin-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions scanWin — crédit de points avec unicité des tickets.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
