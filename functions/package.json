{
  "name": "pharmascan-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions Pharmascan — soumission de reçus pharmacie (unicité, priceEntries, points).",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "test": "node --test"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  }
}
